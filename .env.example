# Frontend build-time configuration (consumed by Vite).
# Copy to .env, .env.production, .env.staging, or .env.local.

# API origin (no trailing slash). Defaults to https://udx-marketplace.store
# if unset so a forgotten env still hits production.
VITE_API_URL=https://udx-marketplace.store
